// import { getServerSession } from "next-auth/next";
// import { authOptions } from "@/app/api/auth/[...nextauth]/route"; // Dein NextAuth-Setup

// // Diese Funktion holt sich die Session des Benutzers
// export default async function auth({ req, res }: any) {
//   return getServerSession(req, res, authOptions); // Hol dir die Session
// }
